<table border="1" cellpadding="10">

    <tr>
        {% for def in definitions %}
            {% if def.association != true or def.associationType != 4 %}
                <td>{{ def.propertyName }}</td>
            {% endif %}
        {% endfor %}
        <td>Actions</td>
    </tr>
    <!-- ### end header -->

    {% for row in data %}
        <tr>
            {% for def in definitions %}

                {% if def.association != true or def.associationType != 4 %}
                    <td>
                        {% include'HackdayScaffoldBundle:Scaffold:blocks/field.html.twig' %}
                    </td>
                {% endif %}

            {% endfor %}
            <td>
                <a href="{{ path(routes.getViewPath(controller), {id:  attribute(row,primaryKey)}) }}">View</a>&nbsp;|&nbsp;
                <a href="{{ path(routes.getEditPath(controller), {id:  attribute(row,primaryKey)}) }}">Edit</a>&nbsp;|&nbsp;
                <a href="{{ path(routes.getDeletePath(controller), {id:  attribute(row,primaryKey)}) }}">Delete</a>
            </td>
        </tr>
    {% endfor %} <!-- ### rows -->

</table>